# read_only

Curated workshop material that Claude must never modify. A `PreToolUse` hook blocks writes anywhere under this directory.

Read freely; cite freely. If something here needs to change, edit it yourself outside Claude Code, or move the file out of `read_only/` first.
